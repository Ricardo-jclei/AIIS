[
  {
    "generation": 1,
    "timestamp": "2026-04-24T19:17:03.868563",
    "execution_time": 1.3200745582580566,
    "generated_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
    "island_results": [
      {
        "island_id": 0,
        "source_island_id": null,
        "score": 0.9056232462519783,
        "sharpe_ratio": 0.9056232462519783,
        "sortino_ratio": 1.527370929084628,
        "max_drawdown": -0.21826385123651115,
        "final_nav": 15814192.576763742,
        "turnover": 0.16118603666799478,
        "weights": [
          0.1783046425480242,
          0.17713272800774577,
          0.17016249554671445,
          0.17879196296729788,
          0.2956081709302178
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 1,
        "source_island_id": null,
        "score": 0.8690566688666207,
        "sharpe_ratio": 0.8690566688666207,
        "sortino_ratio": 1.476089485253744,
        "max_drawdown": -0.22523907580624467,
        "final_nav": 15567158.90103914,
        "turnover": 0.15742792133894457,
        "weights": [
          0.18296281893653274,
          0.1883617326951231,
          0.181742975502691,
          0.17628793335462195,
          0.2706445395110314
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 2,
        "source_island_id": null,
        "score": 0.8790320305546055,
        "sharpe_ratio": 0.8790320305546055,
        "sortino_ratio": 1.4893575400217947,
        "max_drawdown": -0.2232223722989147,
        "final_nav": 15624549.936547462,
        "turnover": 0.15057560106383863,
        "weights": [
          0.18609633683600954,
          0.184229473429186,
          0.17471682380589837,
          0.17664777229690562,
          0.2783095936320004
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 3,
        "source_island_id": null,
        "score": 0.880549238298046,
        "sharpe_ratio": 0.880549238298046,
        "sortino_ratio": 1.4929515133939861,
        "max_drawdown": -0.22241012675909494,
        "final_nav": 15654537.181715008,
        "turnover": 0.15714138751515713,
        "weights": [
          0.17852097599122607,
          0.18748880573372573,
          0.175793097192881,
          0.18084907207741271,
          0.2773480490047544
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 4,
        "source_island_id": null,
        "score": 0.881896711019828,
        "sharpe_ratio": 0.881896711019828,
        "sortino_ratio": 1.4951679902901633,
        "max_drawdown": -0.22268573675901646,
        "final_nav": 15649051.822379166,
        "turnover": 0.15666452877008855,
        "weights": [
          0.1832559408080321,
          0.1866190138543323,
          0.1757641644065046,
          0.17498742617328472,
          0.2793734547578463
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 5,
        "source_island_id": null,
        "score": 0.8416458205843893,
        "sharpe_ratio": 0.8416458205843893,
        "sortino_ratio": 1.4328897562899663,
        "max_drawdown": -0.22900044632474834,
        "final_nav": 15385710.362527648,
        "turnover": 0.15399118989555444,
        "weights": [
          0.1885558816268444,
          0.19266690252796384,
          0.17848034164053228,
          0.18809187943124503,
          0.25220499477341457
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 6,
        "source_island_id": null,
        "score": 0.8537020815545119,
        "sharpe_ratio": 0.8537020815545119,
        "sortino_ratio": 1.4525592011092483,
        "max_drawdown": -0.2274127936294485,
        "final_nav": 15465681.087701343,
        "turnover": 0.1504611370374563,
        "weights": [
          0.1857145388200306,
          0.1925025603895062,
          0.18076983284936357,
          0.18097209998938493,
          0.2600409679517147
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 7,
        "source_island_id": null,
        "score": 0.8566970488077029,
        "sharpe_ratio": 0.8566970488077029,
        "sortino_ratio": 1.4553616416817965,
        "max_drawdown": -0.2279642808635208,
        "final_nav": 15470924.168430863,
        "turnover": 0.14360350673040911,
        "weights": [
          0.19035274788104065,
          0.1806634275993889,
          0.1836169569234733,
          0.18035534791900948,
          0.2650115196770877
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 8,
        "source_island_id": null,
        "score": 0.8527438573106416,
        "sharpe_ratio": 0.8527438573106416,
        "sortino_ratio": 1.451611998533298,
        "max_drawdown": -0.227515430012647,
        "final_nav": 15470843.55567193,
        "turnover": 0.15280879909883593,
        "weights": [
          0.18104446513009617,
          0.1930360871898248,
          0.18371326347544292,
          0.1837248552983987,
          0.2584813289062374
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 9,
        "source_island_id": null,
        "score": 0.8687089077227688,
        "sharpe_ratio": 0.8687089077227688,
        "sortino_ratio": 1.4752397272150874,
        "max_drawdown": -0.22569631290710768,
        "final_nav": 15555988.589120911,
        "turnover": 0.15581055153675138,
        "weights": [
          0.1862200808906153,
          0.1849204570372852,
          0.18221215123664627,
          0.17501069425291976,
          0.2716366165825335
        ],
        "strategy_code": "    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        # multi_factor_array的形状是[时间步, 224]，我们需要提取股票相关的特征\n        # 假设前5个特征对应5个股票\n        if market_state is not None and market_state.shape[1] >= 5:\n            # 使用前5个特征作为股票代理\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    # 基础等权策略\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    # 如果market_state有效，使用更智能的策略\n    if market_state is not None and len(market_state) > 0:\n        # 方法1：基于市场状态的均值策略（使用前5个特征）\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)  # 只使用前5个特征\n            mean_weights = np.clip(mean_weights, 0, None)  # 确保非负\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # 方法2：基于波动率的策略\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0)\n            inv_vol_weights = 1.0 / (volatility + 1e-8)\n            inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        else:\n            inv_vol_weights = base_weights\n        \n        # 方法3：基于趋势的策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            # 限制趋势值范围，避免指数溢出\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(recent_trend)\n            trend_weights = trend_weights / np.sum(trend_weights)\n        else:\n            trend_weights = base_weights\n        \n        # 方法4：动量策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            momentum_weights = np.exp(recent_returns * 5)\n            momentum_weights = momentum_weights / np.sum(momentum_weights)\n        else:\n            momentum_weights = base_weights\n        \n        # 方法5：反转策略（添加数值稳定性）\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            # 计算最近收益\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            # 限制收益值范围\n            recent_returns = np.clip(recent_returns, -1, 1)\n            # 减小放大因子，避免指数溢出\n            reversal_weights = np.exp(-recent_returns * 5)\n            reversal_weights = reversal_weights / np.sum(reversal_weights)\n        else:\n            reversal_weights = base_weights\n        \n        # 组合多种策略（随机选择一种以促进多样性）\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # 添加随机扰动以促进进化\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # 确保权重有效\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    # 如果market_state无效，使用等权策略\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      }
    ],
    "best_sharpe_ratio": 0.9056232462519783,
    "average_sharpe_ratio": 0.8689655610971094,
    "best_island_id": 0,
    "best_final_nav": 15814192.576763742
  },
  {
    "generation": 2,
    "timestamp": "2026-04-24T19:17:46.101576",
    "execution_time": 1.0662145614624023,
    "generated_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
    "island_results": [
      {
        "island_id": 0,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 1,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 2,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 3,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 4,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 5,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 6,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 7,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 8,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 9,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend-following strategy based on recent price movements\n        if market_state.shape[1] >= 5:\n            recent_prices = market_state[-1, :5]\n            historical_prices = market_state[:, :5]\n            price_change = recent_prices - historical_prices[0, :5]\n            price_change = np.clip(price_change, -10, 10)\n            trend_weights = np.exp(np.clip(price_change, -10, 10)) / (np.sum(np.exp(np.clip(price_change, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, 1) / (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(momentum_weights * 5, -10, 10)) / (np.sum(np.exp(np.clip(momentum_weights * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Combining all strategies together\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights) / 4.0\n        combined_weights = np.clip(combined_weights, 0, 1)\n        \n        # Normalize the weights to ensure they sum to 1\n        total_weight = np.sum(combined_weights)\n        final_weights = combined_weights / (total_weight + 1e-8) if total_weight > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      }
    ],
    "best_sharpe_ratio": 0.7545011710060736,
    "average_sharpe_ratio": 0.7545011710060736,
    "best_island_id": 0,
    "best_final_nav": 14788006.149594104
  },
  {
    "generation": 3,
    "timestamp": "2026-04-24T19:17:47.258832",
    "execution_time": 1.1479222774505615,
    "generated_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
    "island_results": [
      {
        "island_id": 0,
        "source_island_id": 6,
        "score": 0.9052321414193668,
        "sharpe_ratio": 0.9052321414193668,
        "sortino_ratio": 1.5267682070636686,
        "max_drawdown": -0.21906784041020558,
        "final_nav": 15807936.070387116,
        "turnover": 0.16347647917730135,
        "weights": [
          0.1787837041243879,
          0.1733260104587855,
          0.17488391833441894,
          0.17666980901997067,
          0.29633655806243697
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 1,
        "source_island_id": 6,
        "score": 0.8458364795925242,
        "sharpe_ratio": 0.8458364795925242,
        "sortino_ratio": 1.4406633089673686,
        "max_drawdown": -0.22943756057387496,
        "final_nav": 15413100.929483194,
        "turnover": 0.15212973060816354,
        "weights": [
          0.18612039911347486,
          0.18706908886866316,
          0.18679360237975035,
          0.18416231382734602,
          0.25585459581076564
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 2,
        "source_island_id": 6,
        "score": 0.8691888834233609,
        "sharpe_ratio": 0.8691888834233609,
        "sortino_ratio": 1.4761398913829704,
        "max_drawdown": -0.22466987161245827,
        "final_nav": 15578280.696621398,
        "turnover": 0.15085694368527963,
        "weights": [
          0.1797249210187604,
          0.18937229088321947,
          0.1801025783927949,
          0.18099133939346557,
          0.2698088703117597
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 3,
        "source_island_id": 6,
        "score": 0.8893915610656062,
        "sharpe_ratio": 0.8893915610656062,
        "sortino_ratio": 1.5066060042438252,
        "max_drawdown": -0.22117099373404991,
        "final_nav": 15705417.91894568,
        "turnover": 0.1588625330340571,
        "weights": [
          0.1797840403678115,
          0.18773325675028885,
          0.17472088802718863,
          0.17423052618241977,
          0.2835312886722912
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 4,
        "source_island_id": 6,
        "score": 0.8880770451992672,
        "sharpe_ratio": 0.8880770451992672,
        "sortino_ratio": 1.5039906409890778,
        "max_drawdown": -0.22115165730873818,
        "final_nav": 15703450.148412112,
        "turnover": 0.16463495033010025,
        "weights": [
          0.17841535721229065,
          0.18112309768229276,
          0.17380886777757504,
          0.18334862582553926,
          0.2833040515023023
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 5,
        "source_island_id": 6,
        "score": 0.8727941143108924,
        "sharpe_ratio": 0.8727941143108924,
        "sortino_ratio": 1.4793138071526373,
        "max_drawdown": -0.22440116399510004,
        "final_nav": 15575291.838599253,
        "turnover": 0.16375254445893997,
        "weights": [
          0.19017835224692137,
          0.18374316064893007,
          0.1742769872957902,
          0.17683241870861754,
          0.2749690810997408
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 6,
        "source_island_id": 6,
        "score": 0.8665742512934559,
        "sharpe_ratio": 0.8665742512934559,
        "sortino_ratio": 1.4704562814335147,
        "max_drawdown": -0.226250024090549,
        "final_nav": 15542685.064932633,
        "turnover": 0.15547218082240508,
        "weights": [
          0.18612078345175384,
          0.18174326831023457,
          0.1839135546163566,
          0.17748739753639606,
          0.270734996085259
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 7,
        "source_island_id": 6,
        "score": 0.8435477678683583,
        "sharpe_ratio": 0.8435477678683583,
        "sortino_ratio": 1.433260213960427,
        "max_drawdown": -0.23014149797960815,
        "final_nav": 15367377.412275977,
        "turnover": 0.15531812577233556,
        "weights": [
          0.19983890679490834,
          0.1793172909583681,
          0.18000949594504087,
          0.18278596376241388,
          0.25804834253926895
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 8,
        "source_island_id": 6,
        "score": 0.8970488692623172,
        "sharpe_ratio": 0.8970488692623172,
        "sortino_ratio": 1.5153951378456867,
        "max_drawdown": -0.22044940934963053,
        "final_nav": 15750909.455485066,
        "turnover": 0.16194553383319676,
        "weights": [
          0.1806203931079392,
          0.17763897189915712,
          0.17574620468593197,
          0.17548054762779886,
          0.29051388267917266
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 9,
        "source_island_id": 6,
        "score": 0.8237603594635734,
        "sharpe_ratio": 0.8237603594635734,
        "sortino_ratio": 1.4075646342088535,
        "max_drawdown": -0.23382244208920253,
        "final_nav": 15259247.486089282,
        "turnover": 0.1521274704257359,
        "weights": [
          0.1910572954512058,
          0.19087771881798735,
          0.19351705804118205,
          0.18303708186740422,
          0.24151084582222063
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        \n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / volatility\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n        \n        recent_trend = market_state[-1, :5] - market_state[0, :5]\n        recent_trend = np.clip(recent_trend, -10, 10)\n        trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        \n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        \n        reversal_returns = -recent_returns\n        reversal_weights = np.exp(np.clip(reversal_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(reversal_returns * 5, -10, 10))) + 1e-8)\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      }
    ],
    "best_sharpe_ratio": 0.9052321414193668,
    "average_sharpe_ratio": 0.8701451472898721,
    "best_island_id": 0,
    "best_final_nav": 15807936.070387116
  },
  {
    "generation": 4,
    "timestamp": "2026-04-24T19:17:48.618379",
    "execution_time": 1.3165080547332764,
    "generated_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
    "island_results": [
      {
        "island_id": 0,
        "source_island_id": 6,
        "score": 0.8438557210352366,
        "sharpe_ratio": 0.8438557210352366,
        "sortino_ratio": 1.4377908766224103,
        "max_drawdown": -0.22944268360511047,
        "final_nav": 15397936.383178215,
        "turnover": 0.15565700109073136,
        "weights": [
          0.18783173677981024,
          0.18887333839356849,
          0.18391491040029884,
          0.18495547370340215,
          0.25442454072292037
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 1,
        "source_island_id": 6,
        "score": 0.845789821234421,
        "sharpe_ratio": 0.845789821234421,
        "sortino_ratio": 1.4402833213354496,
        "max_drawdown": -0.22868703558945014,
        "final_nav": 15419399.945555583,
        "turnover": 0.15863981892215348,
        "weights": [
          0.18448972799645813,
          0.19159706906401386,
          0.1828631746493365,
          0.18646725762719127,
          0.25458277066300034
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 2,
        "source_island_id": 6,
        "score": 0.8569413102047029,
        "sharpe_ratio": 0.8569413102047029,
        "sortino_ratio": 1.4580634718518746,
        "max_drawdown": -0.2276725505656806,
        "final_nav": 15481156.33338487,
        "turnover": 0.15564605518184274,
        "weights": [
          0.18667930544392608,
          0.18569215759073004,
          0.18457705793652604,
          0.1793600895929997,
          0.2636913894358182
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 3,
        "source_island_id": 6,
        "score": 0.8624328350876176,
        "sharpe_ratio": 0.8624328350876176,
        "sortino_ratio": 1.4653382546654945,
        "max_drawdown": -0.22558382497068127,
        "final_nav": 15528110.964701856,
        "turnover": 0.15229463431882145,
        "weights": [
          0.18337899726678472,
          0.19037086295851932,
          0.17767128365394425,
          0.18291782320826372,
          0.2656610329124879
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 4,
        "source_island_id": 6,
        "score": 0.8712173170857737,
        "sharpe_ratio": 0.8712173170857737,
        "sortino_ratio": 1.4782573907258643,
        "max_drawdown": -0.2243513647183915,
        "final_nav": 15579278.413697377,
        "turnover": 0.15979713938663312,
        "weights": [
          0.18481680816799498,
          0.1863161387001019,
          0.17617075850264502,
          0.18021546244817918,
          0.2724808321810789
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 5,
        "source_island_id": 6,
        "score": 0.8501846096022642,
        "sharpe_ratio": 0.8501846096022642,
        "sortino_ratio": 1.4462395562689023,
        "max_drawdown": -0.22870824595509398,
        "final_nav": 15434522.486562524,
        "turnover": 0.15097102783122085,
        "weights": [
          0.18894617749469847,
          0.1835647676256044,
          0.18371483927969828,
          0.18397842960485375,
          0.2597957859951452
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 6,
        "source_island_id": 6,
        "score": 0.8778262121555963,
        "sharpe_ratio": 0.8778262121555963,
        "sortino_ratio": 1.4889270767038258,
        "max_drawdown": -0.22257264809385824,
        "final_nav": 15633081.879029889,
        "turnover": 0.14843726035600655,
        "weights": [
          0.18077850886727154,
          0.1892903805197719,
          0.17293912601926478,
          0.18146560771486153,
          0.27552637687883025
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 7,
        "source_island_id": 6,
        "score": 0.8456311634821705,
        "sharpe_ratio": 0.8456311634821705,
        "sortino_ratio": 1.4372402056697493,
        "max_drawdown": -0.22962799392531408,
        "final_nav": 15400456.846370516,
        "turnover": 0.1467575237510915,
        "weights": [
          0.1913823053623939,
          0.17994336527094656,
          0.18396142775549063,
          0.18696422152899164,
          0.2577486800821773
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 8,
        "source_island_id": 6,
        "score": 0.8583929124457146,
        "sharpe_ratio": 0.8583929124457146,
        "sortino_ratio": 1.4588053609190814,
        "max_drawdown": -0.22682182054210123,
        "final_nav": 15490632.06238094,
        "turnover": 0.16027858231988576,
        "weights": [
          0.18778622062471875,
          0.18560301313858768,
          0.1792194996504154,
          0.18277496470691018,
          0.2646163018793681
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 9,
        "source_island_id": 6,
        "score": 0.8556318383999151,
        "sharpe_ratio": 0.8556318383999151,
        "sortino_ratio": 1.4555582943387828,
        "max_drawdown": -0.22767224704710795,
        "final_nav": 15471772.966032974,
        "turnover": 0.14599079712798213,
        "weights": [
          0.18752464463878782,
          0.18695413271018785,
          0.18283927607626654,
          0.18000024243280097,
          0.2626817041419568
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        mean_weights = np.mean(market_state[:, :5], axis=0)\n        mean_weights = np.clip(mean_weights, 0, None)\n        mean_sum = np.sum(mean_weights)\n        mean_weights = mean_weights / mean_sum if mean_sum > 0 else base_weights\n\n        volatility = np.std(market_state[:, :5], axis=0)\n        inv_vol_weights = 1.0 / (volatility + 1e-8)\n        inv_vol_weights = inv_vol_weights / np.sum(inv_vol_weights)\n\n        if len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        if len(market_state) > 1:\n            recent_returns_reverse = -recent_returns\n            reversal_weights = np.exp(np.clip(recent_returns_reverse * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns_reverse * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        if weights_sum == 0:\n            final_weights = base_weights\n        else:\n            final_weights = final_weights / weights_sum\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      }
    ],
    "best_sharpe_ratio": 0.8778262121555963,
    "average_sharpe_ratio": 0.8567903740733411,
    "best_island_id": 6,
    "best_final_nav": 15633081.879029889
  },
  {
    "generation": 5,
    "timestamp": "2026-04-24T19:17:49.795471",
    "execution_time": 1.1209194660186768,
    "generated_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
    "island_results": [
      {
        "island_id": 0,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 1,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 2,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 3,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 4,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 5,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 6,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 7,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 8,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 9,
        "source_island_id": 6,
        "score": 0.7545011710060736,
        "sharpe_ratio": 0.7545011710060736,
        "sortino_ratio": 1.2957227002180765,
        "max_drawdown": -0.24513042626360598,
        "final_nav": 14788006.149594104,
        "turnover": 0.07641038131040896,
        "weights": [
          0.20679051638551207,
          0.20386423919204633,
          0.1970546391466992,
          0.19749957214862232,
          0.19479103312712
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        trend_weights = np.zeros(5)\n        volatility_weights = np.zeros(5)\n        mean_weights = np.zeros(5)\n        momentum_weights = np.zeros(5)\n        reversal_weights = np.zeros(5)\n\n        if market_state.shape[1] >= 5:\n            # Trend-based weights\n            if len(market_state) > 1:\n                recent_trend = market_state[-1, :5] - market_state[0, :5]\n                recent_trend = np.clip(recent_trend, -10, 10)\n                trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n                trend_weights /= (np.sum(trend_weights) + 1e-8)\n\n            # Volatility-based weights\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            volatility_weights = 1.0 / volatility\n            volatility_weights /= (np.sum(volatility_weights) + 1e-8)\n\n            # Mean-reversion weights\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_weights = mean_prices / current_prices\n            mean_weights = np.clip(mean_weights, 0, 1) / (np.sum(mean_weights) + 1e-8)\n\n            # Momentum weights\n            if len(market_state) > 1:\n                recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns = np.clip(recent_returns, -1, 1)\n                momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n                momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n\n            # Reversal weights\n            if len(market_state) > 1:\n                recent_returns_rev = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n                recent_returns_rev = np.clip(recent_returns_rev, -1, 1)\n                reversal_weights = np.exp(np.clip(-recent_returns_rev * 5, -10, 10))\n                reversal_weights /= (np.sum(reversal_weights) + 1e-8)\n\n        # Combine strategies with dynamic allocation\n        combined_weights = trend_weights * 0.25 + volatility_weights * 0.25 + mean_weights * 0.25 + momentum_weights * 0.25\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      }
    ],
    "best_sharpe_ratio": 0.7545011710060736,
    "average_sharpe_ratio": 0.7545011710060736,
    "best_island_id": 0,
    "best_final_nav": 14788006.149594104
  },
  {
    "generation": 6,
    "timestamp": "2026-04-24T19:18:44.372978",
    "execution_time": 1.0436089038848877,
    "generated_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
    "island_results": [
      {
        "island_id": 0,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 1,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 2,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 3,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 4,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 5,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 6,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 7,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 8,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 9,
        "source_island_id": 8,
        "score": 0.7751071758147545,
        "sharpe_ratio": 0.7751071758147545,
        "sortino_ratio": 1.3280293706068689,
        "max_drawdown": -0.2414849657834244,
        "final_nav": 14924210.117888246,
        "turnover": 0.07108428689774929,
        "weights": [
          0.20465458712255152,
          0.20124768392169443,
          0.19254617979375702,
          0.1925094382264156,
          0.20904211093558145
        ],
        "strategy_code": "    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Trend analysis using recent price movements\n        if market_state.shape[1] >= 5:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10))\n            trend_weights /= (np.sum(trend_weights) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n\n        # Mean-reversion strategy\n        if market_state.shape[1] >= 5:\n            mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n            current_prices = market_state[-1, :5] + 1e-8\n            mean_reversion_weights = mean_prices / current_prices\n            mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n            mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n        else:\n            mean_reversion_weights = base_weights\n\n        # Momentum-based approach\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10))\n            momentum_weights /= (np.sum(momentum_weights) + 1e-8)\n        else:\n            momentum_weights = base_weights\n\n        # Risk control through diverse weighting schemes\n        weights = np.array([trend_weights, inv_vol_weights, mean_reversion_weights, momentum_weights])\n        weights_sum = weights.sum(axis=0)\n        non_zero_weights = np.clip(weights, 0, 1)\n        if np.sum(weights_sum) > 0:\n            final_weights = np.mean(non_zero_weights, axis=0)  # Equal weighting of strategies\n        else:\n            final_weights = base_weights\n\n        # Normalize final weights\n        final_weights = np.clip(final_weights, 0, None)\n        final_weights /= (np.sum(final_weights) + 1e-8)\n\n        return final_weights\n\n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      }
    ],
    "best_sharpe_ratio": 0.7751071758147545,
    "average_sharpe_ratio": 0.7751071758147546,
    "best_island_id": 0,
    "best_final_nav": 14924210.117888246
  },
  {
    "generation": 7,
    "timestamp": "2026-04-24T19:18:45.584051",
    "execution_time": 1.196260929107666,
    "generated_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
    "island_results": [
      {
        "island_id": 0,
        "source_island_id": 8,
        "score": 0.8594260146469266,
        "sharpe_ratio": 0.8594260146469266,
        "sortino_ratio": 1.4602834616287086,
        "max_drawdown": -0.22616037350656074,
        "final_nav": 15507490.853904625,
        "turnover": 0.15308469820781268,
        "weights": [
          0.18430198469831288,
          0.18689559484143983,
          0.1779634566735682,
          0.18651911125598394,
          0.2643198525306952
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 1,
        "source_island_id": 8,
        "score": 0.8705090695927277,
        "sharpe_ratio": 0.8705090695927277,
        "sortino_ratio": 1.4774743132283208,
        "max_drawdown": -0.22446118916957847,
        "final_nav": 15580189.369389508,
        "turnover": 0.1524023993827211,
        "weights": [
          0.1824150645029049,
          0.1884737978457375,
          0.17802965810604146,
          0.17979823342931436,
          0.27128324611600185
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 2,
        "source_island_id": 8,
        "score": 0.8609645774535499,
        "sharpe_ratio": 0.8609645774535499,
        "sortino_ratio": 1.4617968491249305,
        "max_drawdown": -0.22639393973687946,
        "final_nav": 15509203.729580488,
        "turnover": 0.1524608794044724,
        "weights": [
          0.1869528606901945,
          0.18324538435811075,
          0.1790625417990952,
          0.18419580692091989,
          0.26654340623167955
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 3,
        "source_island_id": 8,
        "score": 0.8824572566107237,
        "sharpe_ratio": 0.8824572566107237,
        "sortino_ratio": 1.4951745364313005,
        "max_drawdown": -0.2230537690341077,
        "final_nav": 15661500.563531546,
        "turnover": 0.162221355257882,
        "weights": [
          0.17905026486551268,
          0.18274066858587426,
          0.18146688754015483,
          0.1769272055748613,
          0.27981497343359696
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 4,
        "source_island_id": 8,
        "score": 0.8840761383403123,
        "sharpe_ratio": 0.8840761383403123,
        "sortino_ratio": 1.4977995861022864,
        "max_drawdown": -0.221709630316644,
        "final_nav": 15677592.165291535,
        "turnover": 0.15841984528520656,
        "weights": [
          0.17863059184395264,
          0.18456100722618002,
          0.1739765949695528,
          0.18272365917255637,
          0.280108146787758
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 5,
        "source_island_id": 8,
        "score": 0.8480441354376416,
        "sharpe_ratio": 0.8480441354376416,
        "sortino_ratio": 1.4421014745349463,
        "max_drawdown": -0.22951355704553475,
        "final_nav": 15410859.811188519,
        "turnover": 0.15500821174744603,
        "weights": [
          0.19242836930917778,
          0.18365597496358962,
          0.18506745767472263,
          0.17969343861421294,
          0.259154759438297
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 6,
        "source_island_id": 8,
        "score": 0.8521041831733873,
        "sharpe_ratio": 0.8521041831733873,
        "sortino_ratio": 1.4498434679612564,
        "max_drawdown": -0.2272981419339423,
        "final_nav": 15457276.91143186,
        "turnover": 0.15111730534514609,
        "weights": [
          0.18591721415648502,
          0.19169997674620104,
          0.17822425067939135,
          0.18520364742206333,
          0.25895491099585927
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 7,
        "source_island_id": 8,
        "score": 0.8669126777873495,
        "sharpe_ratio": 0.8669126777873495,
        "sortino_ratio": 1.4711598899727276,
        "max_drawdown": -0.22485436166599201,
        "final_nav": 15548017.82588333,
        "turnover": 0.15569229397219977,
        "weights": [
          0.18704740119189936,
          0.18695424068410338,
          0.17414049492982697,
          0.18205828273151892,
          0.26979958046265123
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 8,
        "source_island_id": 8,
        "score": 0.8647554067409986,
        "sharpe_ratio": 0.8647554067409986,
        "sortino_ratio": 1.4703825724392554,
        "max_drawdown": -0.2256606144139885,
        "final_nav": 15547988.715592781,
        "turnover": 0.14533224805957248,
        "weights": [
          0.18009359208921594,
          0.19125132063978906,
          0.18271231860673323,
          0.17921260449546728,
          0.26673016416879447
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 9,
        "source_island_id": 8,
        "score": 0.837053152976327,
        "sharpe_ratio": 0.837053152976327,
        "sortino_ratio": 1.4263390032391101,
        "max_drawdown": -0.23111324480240902,
        "final_nav": 15337501.542435057,
        "turnover": 0.1518710531007484,
        "weights": [
          0.19461043203408973,
          0.18750802394639787,
          0.18461517002312222,
          0.18180219216105617,
          0.251464181835334
        ],
        "strategy_code": "    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Improved version of `investment_strategy_v0`.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n    \n    if market_state is not None and len(market_state) > 0:\n        # Mean weights\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0)\n            mean_weights = np.clip(mean_weights, 0, None)\n            mean_sum = np.sum(mean_weights)\n            mean_weights = mean_weights / (mean_sum + 1e-8) if mean_sum > 0 else base_weights\n        else:\n            mean_weights = base_weights\n        \n        # Volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = inv_vol_weights / (np.sum(inv_vol_weights) + 1e-8)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Trend-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Momentum-based weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Mean-reversion weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            reversal_weights = np.exp(np.clip(-recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(-recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            reversal_weights = base_weights\n        \n        # Combining strategies\n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, reversal_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        # Ensure valid weights\n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights)\n        final_weights = final_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n        \n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      }
    ],
    "best_sharpe_ratio": 0.8840761383403123,
    "average_sharpe_ratio": 0.8626302612759945,
    "best_island_id": 4,
    "best_final_nav": 15677592.165291535
  },
  {
    "generation": 8,
    "timestamp": "2026-04-24T19:18:46.888127",
    "execution_time": 1.2119383811950684,
    "generated_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
    "island_results": [
      {
        "island_id": 0,
        "source_island_id": 8,
        "score": 0.7342127889675257,
        "sharpe_ratio": 0.7342127889675257,
        "sortino_ratio": 1.262989110149638,
        "max_drawdown": -0.24891604464130607,
        "final_nav": 14648795.820971666,
        "turnover": 0.11401635275880012,
        "weights": [
          0.21093020576499086,
          0.20405849131878737,
          0.20133656317676296,
          0.20225386598152795,
          0.181420873757931
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 1,
        "source_island_id": 8,
        "score": 0.7385861704456169,
        "sharpe_ratio": 0.7385861704456169,
        "sortino_ratio": 1.2692030166844794,
        "max_drawdown": -0.24782516908042196,
        "final_nav": 14689006.177604552,
        "turnover": 0.11045799106078079,
        "weights": [
          0.2055057957591215,
          0.20272173972205879,
          0.20107952125937506,
          0.2073033411411106,
          0.18338960211833405
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 2,
        "source_island_id": 8,
        "score": 0.7330281970973486,
        "sharpe_ratio": 0.7330281970973486,
        "sortino_ratio": 1.2612469426669184,
        "max_drawdown": -0.24918420618166712,
        "final_nav": 14644193.907601742,
        "turnover": 0.11399757127403896,
        "weights": [
          0.2091758255417055,
          0.20459458489529683,
          0.20319218054207214,
          0.202880124681215,
          0.1801572843397106
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 3,
        "source_island_id": 8,
        "score": 0.7370662738567076,
        "sharpe_ratio": 0.7370662738567076,
        "sortino_ratio": 1.2676764253918869,
        "max_drawdown": -0.24868948339722868,
        "final_nav": 14669165.457142906,
        "turnover": 0.11495503482948947,
        "weights": [
          0.2093327952981025,
          0.2023152980890269,
          0.2034040945006731,
          0.20151538425065044,
          0.18343242786154693
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 4,
        "source_island_id": 8,
        "score": 0.7636483542551036,
        "sharpe_ratio": 0.7636483542551036,
        "sortino_ratio": 1.3102281513480813,
        "max_drawdown": -0.24314562387175925,
        "final_nav": 14860426.422802549,
        "turnover": 0.12473733631882468,
        "weights": [
          0.20071727895992178,
          0.2041049184180963,
          0.19575554358846595,
          0.1996720337595259,
          0.19975022527399022
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 5,
        "source_island_id": 8,
        "score": 0.7492811045644199,
        "sharpe_ratio": 0.7492811045644199,
        "sortino_ratio": 1.2878575691380638,
        "max_drawdown": -0.24557297305187908,
        "final_nav": 14760465.726080569,
        "turnover": 0.11085202342448056,
        "weights": [
          0.20476135511450452,
          0.20652598991560653,
          0.19649861987668998,
          0.2020440796756844,
          0.1901699554175145
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 6,
        "source_island_id": 8,
        "score": 0.7231921834937655,
        "sharpe_ratio": 0.7231921834937655,
        "sortino_ratio": 1.2451821731275123,
        "max_drawdown": -0.2504618895656124,
        "final_nav": 14579349.485069951,
        "turnover": 0.11147371719500143,
        "weights": [
          0.210808487428102,
          0.20795421727407584,
          0.2017429507914464,
          0.2065943720060488,
          0.17289997250032685
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 7,
        "source_island_id": 8,
        "score": 0.7521467741358108,
        "sharpe_ratio": 0.7521467741358108,
        "sortino_ratio": 1.2929152978189042,
        "max_drawdown": -0.24489047133667574,
        "final_nav": 14786886.441851042,
        "turnover": 0.11773230732840033,
        "weights": [
          0.20087220084145205,
          0.21114101642493907,
          0.19734995406687364,
          0.19989546094078164,
          0.19074136772595363
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 8,
        "source_island_id": 8,
        "score": 0.761311719806186,
        "sharpe_ratio": 0.761311719806186,
        "sortino_ratio": 1.3058340410876035,
        "max_drawdown": -0.24390585941859683,
        "final_nav": 14829972.8547145,
        "turnover": 0.12482571831415325,
        "weights": [
          0.20780815720815995,
          0.20104238277325817,
          0.19417747111980124,
          0.19685623888445813,
          0.20011575001432252
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 9,
        "source_island_id": 8,
        "score": 0.7603499218672575,
        "sharpe_ratio": 0.7603499218672575,
        "sortino_ratio": 1.3057025197283525,
        "max_drawdown": -0.24429553576939766,
        "final_nav": 14841431.24200349,
        "turnover": 0.12120945299869268,
        "weights": [
          0.19856447353476034,
          0.20088340884186087,
          0.20175069630840334,
          0.20115844902761296,
          0.19764297228736263
        ],
        "strategy_code": "    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    \"\"\"Enhanced version of investment strategy with diverse methods.\"\"\"\n    if portfolio is None:\n        if market_state is not None and market_state.shape[1] >= 5:\n            portfolio = market_state[-1, :5] if len(market_state) > 0 else np.ones(5)\n        else:\n            portfolio = np.ones(5)\n    \n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        # Method 1: Mean weights with adjustment\n        if market_state.shape[1] >= 5:\n            mean_weights = np.mean(market_state[:, :5], axis=0) + 1e-8\n            mean_sum = np.sum(mean_weights)\n            mean_weights = np.clip(mean_weights / mean_sum, 0, 1)\n        else:\n            mean_weights = base_weights\n        \n        # Method 2: Inverse volatility weights\n        if market_state.shape[1] >= 5:\n            volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n            inv_vol_weights = 1.0 / volatility\n            inv_vol_weights = np.clip(inv_vol_weights / np.sum(inv_vol_weights), 0, 1)\n        else:\n            inv_vol_weights = base_weights\n        \n        # Method 3: Trend-following strategy with caps\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n        \n        # Method 4: Momentum weights\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n            recent_returns = np.clip(recent_returns, -1, 1)\n            momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n        else:\n            momentum_weights = base_weights\n        \n        # Method 5: Risk-adjusted weights based on recent performance\n        if market_state.shape[1] >= 5 and len(market_state) > 1:\n            recent_performance = (market_state[-1, :5] / (market_state[-2, :5] + 1e-8)) - 1\n            recent_performance = np.clip(recent_performance, -1, 1)\n            risk_adjusted_weights = np.exp(np.clip(-recent_performance, -10, 10)) / (np.sum(np.exp(np.clip(-recent_performance, -10, 10))) + 1e-8)\n        else:\n            risk_adjusted_weights = base_weights\n        \n        strategies = [mean_weights, inv_vol_weights, trend_weights, momentum_weights, risk_adjusted_weights, base_weights]\n        selected_strategy = np.random.choice(len(strategies))\n        \n        # Add some exploration noise\n        noise = np.random.normal(0, 0.05, len(portfolio))\n        final_weights = strategies[selected_strategy] + noise\n        \n        final_weights = np.clip(final_weights, 0, 1)\n        weights_sum = np.sum(final_weights) + 1e-8\n        final_weights = final_weights / weights_sum\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      }
    ],
    "best_sharpe_ratio": 0.7636483542551036,
    "average_sharpe_ratio": 0.7452823488489743,
    "best_island_id": 4,
    "best_final_nav": 14860426.422802549
  },
  {
    "generation": 9,
    "timestamp": "2026-04-24T19:18:48.112351",
    "execution_time": 1.1123480796813965,
    "generated_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
    "island_results": [
      {
        "island_id": 0,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 1,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 2,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 3,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 4,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 5,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 6,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 7,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 8,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      },
      {
        "island_id": 9,
        "source_island_id": 8,
        "score": 0.7563842929140105,
        "sharpe_ratio": 0.7563842929140105,
        "sortino_ratio": 1.2995592349114398,
        "max_drawdown": -0.2447551537116378,
        "final_nav": 14804386.778134957,
        "turnover": 0.0544484084408793,
        "weights": [
          0.2046863467955248,
          0.20433113365412767,
          0.1975923610345877,
          0.1977961948001531,
          0.1955939637156069
        ],
        "strategy_code": "    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights",
        "evaluated_program": "\"\"\"Enhanced FunSearch specification with full evaluation and visualization.\"\"\"\n\nimport numpy as np\nimport torch\nimport os\nimport sys\nimport pandas as pd\nimport matplotlib\nimport json\nimport platform\nimport yaml\nfrom datetime import datetime\n\n# ====== 0. Set matplotlib font to avoid Colab warnings ======\nmatplotlib.rcParams['font.family'] = 'sans-serif'\nmatplotlib.rcParams['font.sans-serif'] = ['DejaVu Sans', 'Arial', 'Helvetica', 'sans-serif']\nmatplotlib.rcParams['axes.unicode_minus'] = False\n\n# 添加项目路径\nsys.path.append(os.path.abspath(os.path.dirname(__file__)))\n\nfrom src.model.enhanced_lstm import EnhancedLSTMModel\nfrom src.eval.metrics import sharpe_ratio, sortino_ratio, max_drawdown\nfrom src.rl.multi_asset_trading_env import MultiAssetTradingEnv\nfrom stable_baselines3 import PPO\n\n\n# ====== 1. 加载LSTM模型 ======\nlstm_model_path = 'model_ckpt/best_lstm_multi_asset.pth'\nlstm_input_size = 224\nlstm_hidden_size = 128\nlstm_num_layers = 3\nlstm_output_size = 224\nlstm_model = EnhancedLSTMModel(lstm_input_size, lstm_hidden_size, lstm_num_layers, lstm_output_size, 0.3)\nlstm_model.load_state_dict(torch.load(lstm_model_path, map_location='cpu'))\nlstm_model.eval()\n\n\n# ====== 2. 加载数据 ======\nstock_list = ['600519', '600030', '600036', '601318', '601988']\nmf_data_dir_tpl = 'data/processed/{}/multi_factor.csv'\nmf_dfs = []\nfor code in stock_list:\n    path = mf_data_dir_tpl.format(code)\n    df = pd.read_csv(path)\n    df = df.rename(columns={col: f'{code}_{col}' for col in df.columns if col != '日期'})\n    mf_dfs.append(df)\nmf_df = mf_dfs[0]\nfor df in mf_dfs[1:]:\n    mf_df = mf_df.merge(df, on='日期', how='inner')\nmulti_factor_array = mf_df.drop(columns=['日期']).values\n\n# 加载价格数据\ndata_dir_tpl = 'data/processed/{}/market/daily/20220425_20250424_processed.csv'\nprice_dfs = []\nfor code in stock_list:\n    path = data_dir_tpl.format(code)\n    if not os.path.exists(path):\n        # 尝试其他可能的路径\n        alt_path = f'data/processed/{code}/20220425_20250424_processed.csv'\n        if os.path.exists(alt_path):\n            path = alt_path\n        else:\n            raise FileNotFoundError(f\"价格数据文件不存在: {path}\")\n    df = pd.read_csv(path, usecols=['日期', '收盘'])\n    df = df.rename(columns={'收盘': code})\n    price_dfs.append(df)\n\n# 合并价格数据\nprice_df = price_dfs[0]\nfor df in price_dfs[1:]:\n    price_df = price_df.merge(df, on='日期', how='inner')\n\n# 确保数据有效\nif price_df.empty:\n    raise ValueError(\"价格数据合并后为空\")\n\nprice_array = price_df[stock_list].values\nprint(f\"价格数据加载完成，形状: {price_array.shape}\")\n\n\n# ====== 3. 定义funsearch装饰器 ======\nclass funsearch:\n    @staticmethod\n    def run(func):\n        return func\n    \n    @staticmethod\n    def evolve(func):\n        return func\n\n\n# ====== 4. 回测函数（与user_demo.py一致） ======\ndef backtest(weights, price_array, initial_cash=10000000.0, fee_rate=0.001, slippage_rate=0.0):\n    \"\"\"\n    Backtest strategy and return NAV and metrics.\n    \n    Args:\n        weights: Strategy weights\n        price_array: Price array\n        initial_cash: Initial cash\n        fee_rate: Transaction fee rate\n        slippage_rate: Slippage rate\n    \n    Returns:\n        nav: Net asset value\n        returns: Returns\n        sr: Sharpe ratio\n        so: Sortino ratio\n        mdd: Max drawdown\n    \"\"\"\n    if price_array is None or len(price_array) == 0:\n        return np.array([initial_cash]), np.array([]), 0.0, 0.0, 0.0\n    \n    if weights is None:\n        weights = np.ones(price_array.shape[1]) / price_array.shape[1]\n    \n    # 确保权重归一化\n    weights = np.clip(weights, 0, 1)\n    weights_sum = np.sum(weights)\n    if weights_sum == 0:\n        weights = np.ones(len(weights)) / len(weights)\n    else:\n        weights = weights / weights_sum\n    \n    nav = [initial_cash]\n    \n    for t in range(1, len(price_array)):\n        try:\n            # 计算收益率\n            price_change = (price_array[t] - price_array[t-1]) / (price_array[t-1] + 1e-8)\n            ret = np.dot(price_change, weights)\n            \n            # 调试：检查 ret 是否有效\n            if isinstance(ret, (np.ndarray, list)):\n                if hasattr(ret, 'shape') and len(ret.shape) > 0 and ret.shape[0] > 1:\n                    print(f\"[backtest] ret is array with shape {ret.shape} at t={t}，使用第一个元素\")\n                    ret = ret[0]\n            \n            # 应用滑点\n            if slippage_rate > 0:\n                # 假设滑点对收益率的影响\n                ret = ret * (1 - slippage_rate)\n            \n            nav_value = nav[-1] * (1 + ret)\n            \n            # 调试：检查 nav_value 是否有效\n            if isinstance(nav_value, (np.ndarray, list)):\n                print(f\"[backtest] nav_value is array with shape {nav_value.shape} at t={t}，使用标量值\")\n                nav_value = float(nav_value.flatten()[0]) if hasattr(nav_value, 'size') and nav_value.size > 0 else nav[-1]\n            \n            if np.isnan(nav_value) or np.isinf(nav_value):\n                print(f\"[backtest] nav_value is NaN or Inf at t={t}，使用前一个值\")\n                nav_value = nav[-1]\n            \n            nav.append(nav_value)\n        except Exception as e:\n            print(f\"[backtest] 计算错误: {e} at t={t}，使用前一个值\")\n            nav.append(nav[-1])\n\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1] if len(nav) > 1 else np.array([])\n\n    # Calculate metrics\n    sr = sharpe_ratio(returns) if len(returns) > 0 else 0.0\n    so = sortino_ratio(returns) if len(returns) > 0 else 0.0\n    mdd = max_drawdown(nav) if len(nav) > 0 else 0.0\n\n    return nav, returns, sr, so, mdd\n\n\ndef minvar_weights(price_array):\n    \"\"\"Minimum variance portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ np.ones(len(stock_list))\n    w = w / np.sum(w)\n    return w\n\n\ndef maxsharpe_weights(price_array):\n    \"\"\"Maximum Sharpe ratio portfolio.\"\"\"\n    returns = (price_array[1:] - price_array[:-1]) / price_array[:-1]\n    mean_ret = np.mean(returns, axis=0)\n    cov = np.cov(returns.T)\n    inv_cov = np.linalg.pinv(cov)\n    w = inv_cov @ mean_ret\n    w = w / np.sum(w)\n    return w\n\n\ndef lstm_ppo_weights(multi_factor_array, lstm_model, ppo_model, window_size=20):\n    \"\"\"LSTM+PPO静态权重（仅用于展示）\"\"\"\n    N = 5\n    position = np.zeros(N)\n    cash = 1e7\n    weights_list = []\n    for t in range(window_size, len(multi_factor_array)):\n        window = multi_factor_array[t-window_size:t]\n        window_norm = (window - np.mean(window, axis=0)) / (np.std(window, axis=0) + 1e-8)\n        X = torch.tensor(window_norm, dtype=torch.float32).unsqueeze(0)\n        with torch.no_grad():\n            feats = lstm_model(X).cpu().numpy().flatten()\n        prices = price_array[t-1]\n        total_value = cash + np.sum(position * prices)\n        position_ratio = (position * prices) / (total_value + 1e-8)\n        cash_ratio = cash / (total_value + 1e-8)\n        state = np.concatenate([feats, position_ratio, [cash_ratio]]).astype(np.float32)\n        action, _ = ppo_model.predict(state, deterministic=True)\n        w = np.clip(action, 0, 1)\n        w = w / (np.sum(w) + 1e-8)\n        weights_list.append(w)\n    weights = np.mean(np.array(weights_list), axis=0)\n    return weights\n\n\ndef lstm_ppo_dynamic_backtest(price_array, feature_array, ppo_model, window_size=20, sharpe_window=20, lstm_input_size=224):\n    \"\"\"LSTM+PPO动态RL推理（真实RL策略回测）\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'src/config.yaml')\n    if not os.path.exists(config_path):\n        config_path = os.path.join(os.path.dirname(__file__), '../src/config.yaml')\n    if os.path.exists(config_path):\n        with open(config_path, 'r') as f:\n            config = yaml.safe_load(f)\n    else:\n        config = {}\n    fee_rate = config.get('fee_rate', 0.001)\n    slippage_rate = config.get('slippage_rate', 0.0)\n    enable_slippage = config.get('enable_slippage', False)\n    \n    env = MultiAssetTradingEnv(\n        price_array=price_array,\n        feature_array=feature_array,\n        window_size=window_size,\n        sharpe_window=sharpe_window,\n        reward_type='sharpe_turnover_penalty',\n        lstm_input_size=lstm_input_size,\n        fee_rate=fee_rate,\n        slippage_rate=slippage_rate,\n        enable_slippage=enable_slippage\n    )\n    obs, info = env.reset()\n    nav = [env.initial_cash]\n    done = False\n    while not done:\n        action, _ = ppo_model.predict(obs, deterministic=True)\n        obs, reward, terminated, truncated, info = env.step(action)\n        nav.append(env.portfolio_value)\n        done = terminated or truncated\n    nav = np.array(nav)\n    returns = (nav[1:] - nav[:-1]) / nav[:-1]\n    sharpe = sharpe_ratio(returns)\n    sortino = sortino_ratio(returns)\n    calmar = -max_drawdown(nav)\n    return nav, sharpe, sortino, calmar\n\n\ndef evaluate_strategy(test_input: dict) -> dict:\n    \"\"\"\n    Enhanced evaluation function that returns comprehensive metrics.\n\n    Args:\n        test_input: Contains 'strategy_func', 'window_size', and 'island_id'\n\n    Returns:\n        Dictionary containing all performance metrics:\n        - sharpe_ratio: Sharpe ratio (used for evolution)\n        - sortino_ratio: Sortino ratio\n        - max_drawdown: Maximum drawdown\n        - final_nav: Final net asset value\n        - turnover: Portfolio turnover rate\n        - weights: Average portfolio weights\n    \"\"\"\n    strategy_func = test_input['strategy_func']\n    window_size = test_input['window_size']\n    island_id = test_input.get('island_id', 0)\n    \n    # 只在第一个island打印策略信息\n    if island_id == 0:\n            print(f\"[Evaluator] 策略信息:\")\n            print(f\"   策略函数: {strategy_func.__name__}\")\n            print(f\"   策略模块: {strategy_func.__module__}\")\n            # 重置结果列表\n            global island_results\n            island_results = []\n    \n    # 使用进化策略进行回测\n    # 生成策略权重序列\n    weights_list = []\n    error_count = 0\n    max_errors = 5  # 最大错误打印次数\n    fallback_count = 0  # 跟踪等权回退次数\n    \n    for t in range(window_size, len(multi_factor_array)):\n        try:\n            weights_t = strategy_func(multi_factor_array[t-window_size:t], None)\n            \n            # 检查 weights_t 是否有效\n            if weights_t is None:\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t is None at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif not isinstance(weights_t, np.ndarray):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t type error: {type(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif len(weights_t) != len(stock_list):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t length error: {len(weights_t)} at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            elif np.any(np.isnan(weights_t)) or np.any(np.isinf(weights_t)):\n                if error_count < max_errors:\n                    print(f\"[evaluate_strategy] weights_t contains NaN or Inf at t={t}，使用等权回退\")\n                    error_count += 1\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            \n            # 确保权重归一化\n            weights_t = np.clip(weights_t, 0, 1)\n            weights_sum = np.sum(weights_t)\n            if weights_sum == 0:\n                weights_t = np.ones(len(stock_list)) / len(stock_list)\n                fallback_count += 1\n            else:\n                weights_t = weights_t / weights_sum\n            \n            weights_list.append(weights_t)\n        except Exception as e:\n            if error_count < max_errors:\n                print(f\"[evaluate_strategy] 策略执行错误: {e} at t={t}，使用等权回退\")\n                error_count += 1\n            weights_t = np.ones(len(stock_list)) / len(stock_list)\n            fallback_count += 1\n            weights_list.append(weights_t)\n            \n    # 计算平均权重作为静态策略\n    if len(weights_list) == 0:\n        print(f\"[evaluate_strategy] weights_list is empty，使用等权\")\n        weights = np.ones(len(stock_list)) / len(stock_list)\n    else:\n        weights_array = np.array(weights_list)\n        if np.any(np.isnan(weights_array)) or np.any(np.isinf(weights_array)):\n            print(f\"[evaluate_strategy] weights_list contains NaN or Inf，使用等权回退\")\n            weights = np.ones(len(stock_list)) / len(stock_list)\n        else:\n            weights = np.mean(weights_array, axis=0)\n            \n            # 验证 weights\n            if np.any(np.isnan(weights)) or np.any(np.isinf(weights)):\n                print(f\"[evaluate_strategy] weights contains NaN or Inf，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            elif weights is None:\n                print(f\"[evaluate_strategy] weights is None，使用等权回退\")\n                weights = np.ones(len(stock_list)) / len(stock_list)\n            else:\n                # 确保权重归一化\n                weights = np.clip(weights, 0, 1)\n                weights_sum = np.sum(weights)\n                if weights_sum == 0:\n                    weights = np.ones(len(stock_list)) / len(stock_list)\n                else:\n                    weights = weights / weights_sum\n    \n    # Backtest\n    nav, returns, sr, so, mdd = backtest(weights, price_array, fee_rate=0.001, slippage_rate=0.0)\n    \n    # Calculate turnover\n    positions = np.array(weights_list)\n    turnover = np.mean(np.abs(np.diff(positions, axis=0))) if len(positions) > 1 else 0.0\n    \n    # 统计有效权重数量\n    valid_weights_count = len([w for w in weights_list if not np.any(np.isnan(w)) and not np.any(np.isinf(w))])\n    if island_id == 0:\n        print(f\"   有效权重数量: {valid_weights_count}/{len(weights_list)}\")\n        print(f\"   等权回退次数: {fallback_count}\")\n    \n    # 只在第一个island打印详细信息\n    if island_id == 0:\n        # 打印前几个权重作为示例\n        if len(weights_list) > 0:\n            print(f\"   首个权重: {weights_list[0]}\")\n            print(f\"   权重数量: {len(weights_list)}\")\n    \n    # 存储评估结果\n    island_results.append({\n        'island_id': island_id,\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'turnover': turnover,\n        'final_nav': nav[-1]\n    })\n    \n    # 当处理完所有island后，打印表格\n    # 假设一轮有10个island\n    if island_id == 9:  # 最后一个island\n        print(\"\\n\" + \"=\"*80)\n        print(\"本轮评估结果汇总\")\n        print(\"=\"*80)\n        # 打印表头\n        print(f\"{'Island':<8} {'夏普比率':<10} {'索提诺比率':<10} {'最大回撤':<10} {'换手率':<10} {'最终净值':<12}\")\n        print(\"-\"*80)\n        # 打印每个island的结果\n        for result in island_results:\n            print(f\"{result['island_id']:<8} {result['sharpe_ratio']:.4f}     {result['sortino_ratio']:.4f}     {result['max_drawdown']:.4f}     {result['turnover']:.4f}     {result['final_nav']:.2f}\")\n        print(\"=\"*80 + \"\\n\")\n\n    # Return dictionary with all metrics for comprehensive evaluation\n    return {\n        'sharpe_ratio': sr,\n        'sortino_ratio': so,\n        'max_drawdown': mdd,\n        'final_nav': nav[-1],\n        'turnover': turnover,\n        'weights': weights.tolist()\n    }\n\n\ndef investment_strategy(market_state: np.ndarray, portfolio: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Enhanced initial strategy template with diversity to promote evolution.\n    \n    Args:\n        market_state: Current market state array (shape: [window_size, 224])\n        portfolio: Current portfolio weights (shape: [5])\n    \n    Returns:\n        New portfolio weights (shape: [5])\n    \"\"\"\n    if portfolio is None:\n        portfolio = market_state[-1, :5] if market_state is not None and market_state.shape[1] >= 5 else np.ones(5)\n\n    base_weights = np.ones(len(portfolio)) / len(portfolio)\n\n    if market_state is not None and len(market_state) > 0:\n        if market_state.shape[1] >= 5:\n            # Trend-following strategy\n            recent_trend = market_state[-1, :5] - market_state[0, :5]\n            recent_trend = np.clip(recent_trend, -10, 10)\n            trend_weights = np.exp(np.clip(recent_trend, -10, 10)) / (np.sum(np.exp(np.clip(recent_trend, -10, 10))) + 1e-8)\n        else:\n            trend_weights = base_weights\n\n        # Volatility-based weighting\n        volatility = np.std(market_state[:, :5], axis=0) + 1e-8\n        inv_vol_weights = 1.0 / (volatility)\n        inv_vol_weights /= (np.sum(inv_vol_weights) + 1e-8)\n\n        # Mean-reversion strategy\n        mean_prices = np.mean(market_state[:, :5], axis=0) + 1e-8\n        current_prices = market_state[-1, :5] + 1e-8\n        mean_reversion_weights = mean_prices / current_prices\n        mean_reversion_weights = np.clip(mean_reversion_weights, 0, None)\n        mean_reversion_weights /= (np.sum(mean_reversion_weights) + 1e-8)\n\n        # Momentum-based strategy\n        recent_returns = (market_state[-1, :5] - market_state[-2, :5]) / (market_state[-2, :5] + 1e-8)\n        recent_returns = np.clip(recent_returns, -1, 1)\n        momentum_weights = np.exp(np.clip(recent_returns * 5, -10, 10)) / (np.sum(np.exp(np.clip(recent_returns * 5, -10, 10))) + 1e-8)\n\n        # Risk control\n        risk_weights = np.sqrt(1 / (volatility + 1e-8))\n        risk_weights /= (np.sum(risk_weights) + 1e-8)\n\n        # Combine strategies\n        combined_weights = (trend_weights + inv_vol_weights + mean_reversion_weights + momentum_weights + risk_weights) / 5\n        combined_weights = np.clip(combined_weights, 0, 1)\n        weights_sum = np.sum(combined_weights)\n        final_weights = combined_weights / (weights_sum + 1e-8) if weights_sum > 0 else base_weights\n\n        return final_weights\n    \n    return base_weights\n\n\ndef plot_comparison(nav_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Plot comparison of different strategies.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    color_map = {\n        'Equal Weight': '#1f77b4',\n        'Minimum Variance': '#2ca02c',\n        'Maximum Sharpe': '#d62728',\n        'LSTM+PPO': '#7f7f7f',\n        'LSTM+PPO Dynamic RL': '#ff7f0e',\n        'PPO Dynamic RL (No LSTM)': '#800080',\n        'FunSearch': '#e377c2'\n    }\n    \n    import matplotlib.pyplot as plt\n    \n    # NAV curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            plt.plot(nav, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Net Asset Value', fontsize=14)\n    plt.title('Strategy NAV Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_nav_compare.png', dpi=180)\n    plt.close()\n    \n    # Drawdown curve\n    plt.figure(figsize=(12, 7))\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 0:\n            peak = np.maximum.accumulate(nav)\n            drawdown = (nav - peak) / (peak + 1e-8)\n            plt.plot(drawdown, label=name, linewidth=2, color=color_map.get(name, None))\n    \n    plt.legend(fontsize=13)\n    plt.xlabel('Time Steps', fontsize=14)\n    plt.ylabel('Drawdown', fontsize=14)\n    plt.title('Strategy Drawdown Comparison', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/portfolio_drawdown_compare.png', dpi=180)\n    plt.close()\n\n\ndef plot_evolution_progress(log_dir='funsearch_evolution', output_dir='funsearch_results'):\n    \"\"\"\n    Plot evolution progress from log files.\n    \n    Args:\n        log_dir: Directory containing evolution logs\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    # Load evolution logs\n    log_file = os.path.join(log_dir, 'evolution_log.json')\n    if not os.path.exists(log_file):\n        print(f\"Evolution log file not found: {log_file}\")\n        return\n    \n    try:\n        with open(log_file, 'r', encoding='utf-8') as f:\n            evolution_log = json.load(f)\n    except Exception as e:\n        print(f\"Failed to load evolution log: {e}\")\n        return\n    \n    if not evolution_log:\n        print(\"Evolution log is empty\")\n        return\n    \n    import matplotlib.pyplot as plt\n    \n    # Extract data\n    generations = []\n    best_scores = []\n    average_scores = []\n    execution_times = []\n    \n    for entry in evolution_log:\n        generations.append(entry.get('generation', 0))\n        best_scores.append(entry.get('best_sharpe_ratio', entry.get('best_score', 0)))\n        average_scores.append(entry.get('average_sharpe_ratio', entry.get('average_score', 0)))\n        execution_times.append(entry.get('execution_time', 0))\n    \n    # Evolution score trend\n    plt.figure(figsize=(12, 7))\n    plt.plot(generations, best_scores, 'o-', label='Best Score', linewidth=2, color='#d62728')\n    plt.plot(generations, average_scores, 's-', label='Average Score', linewidth=2, color='#1f77b4')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Score (Sharpe Ratio)', fontsize=14)\n    plt.title('FunSearch Evolution Progress', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_progress.png', dpi=180)\n    plt.close()\n    \n    # Execution time trend\n    plt.figure(figsize=(12, 5))\n    plt.plot(generations, execution_times, '^-', label='Execution Time', linewidth=2, color='#2ca02c')\n    plt.legend(fontsize=13)\n    plt.xlabel('Generation', fontsize=14)\n    plt.ylabel('Execution Time (seconds)', fontsize=14)\n    plt.title('Execution Time per Generation', fontsize=16)\n    plt.grid(True, alpha=0.3)\n    plt.tight_layout()\n    plt.savefig(f'{output_dir}/evolution_execution_time.png', dpi=180)\n    plt.close()\n    \n    print(f\"Evolution progress visualization saved to: {output_dir}/\")\n\n\ndef generate_report(nav_dict, metrics_dict, output_dir='funsearch_results'):\n    \"\"\"\n    Generate markdown report.\n    \n    Args:\n        nav_dict: Dictionary of {strategy_name: nav_array}\n        metrics_dict: Dictionary of {strategy_name: {metric: value}}\n        output_dir: Output directory\n    \"\"\"\n    os.makedirs(output_dir, exist_ok=True)\n    \n    report_path = f'{output_dir}/funsearch_report.md'\n    \n    # Calculate additional performance metrics\n    extended_metrics = {}\n    for name, nav in nav_dict.items():\n        if nav is not None and len(nav) > 1:\n            returns = (nav[1:] - nav[:-1]) / nav[:-1]\n            sr = sharpe_ratio(returns)\n            so = sortino_ratio(returns)\n            mdd = max_drawdown(nav)\n            # Calculate annualized return\n            annual_return = (nav[-1] / nav[0]) ** (252 / len(returns)) - 1 if len(returns) > 0 else 0\n            # Calculate Calmar ratio\n            calmar_ratio = annual_return / (-mdd) if mdd < 0 else 0\n            # Calculate win rate\n            win_rate = np.mean(returns > 0) if len(returns) > 0 else 0\n            \n            extended_metrics[name] = {\n                'annual_return': annual_return,\n                'sharpe_ratio': sr,\n                'sortino_ratio': so,\n                'calmar_ratio': calmar_ratio,\n                'max_drawdown': mdd,\n                'win_rate': win_rate\n            }\n        else:\n            extended_metrics[name] = {\n                'annual_return': 0,\n                'sharpe_ratio': 0,\n                'sortino_ratio': 0,\n                'calmar_ratio': 0,\n                'max_drawdown': 0,\n                'win_rate': 0\n            }\n    \n    with open(report_path, 'w', encoding='utf-8') as f:\n        f.write('# FunSearch Portfolio Optimization Report\\n\\n')\n        f.write(f'Generated at: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")}\\n\\n')\n        \n        f.write('## Strategy Overview\\n\\n')\n        f.write(f'- Number of assets: {len(stock_list)}\\n')\n        f.write(f'- Backtest period: {len(next(iter(nav_dict.values())))} time steps\\n')\n        f.write(f'- Stock list: {stock_list}\\n\\n')\n        \n        f.write('## Performance Metrics Comparison\\n\\n')\n        f.write('| Strategy | Annual Return | Sharpe Ratio | Sortino Ratio | Calmar Ratio | Max Drawdown | Win Rate |\\n')\n        f.write('|---|---|---|---|---|---|---|\\n')\n        \n        for name, metrics in extended_metrics.items():\n            f.write(f'| {name} | {metrics.get(\"annual_return\", 0):.4f} | {metrics.get(\"sharpe_ratio\", 0):.4f} | {metrics.get(\"sortino_ratio\", 0):.4f} | {metrics.get(\"calmar_ratio\", 0):.4f} | {metrics.get(\"max_drawdown\", 0):.4f} | {metrics.get(\"win_rate\", 0):.4f} |\\n')\n        \n        f.write('\\n## NAV Curve\\n\\n')\n        f.write(f'![NAV Curve](portfolio_nav_compare.png)\\n\\n')\n        \n        f.write('## Drawdown Curve\\n\\n')\n        f.write(f'![Drawdown Curve](portfolio_drawdown_compare.png)\\n\\n')\n        \n        f.write('## Return Distribution\\n\\n')\n        f.write(f'![Return Distribution](portfolio_returns_distribution.png)\\n\\n')\n        \n        f.write('## Performance Metrics Radar\\n\\n')\n        f.write(f'![Performance Metrics Radar](portfolio_metrics_radar.png)\\n\\n')\n        \n        f.write('## Weight Distribution\\n\\n')\n        f.write(f'![Weight Distribution](portfolio_weights_compare.png)\\n\\n')\n        \n        f.write('## Strategy Analysis\\n\\n')\n        f.write('### Best Strategy\\n')\n        # Find strategy with highest Sharpe ratio\n        best_strategy = max(extended_metrics, key=lambda x: extended_metrics[x]['sharpe_ratio'])\n        f.write(f'- Best strategy: {best_strategy}\\n')\n        f.write(f'- Annual return: {extended_metrics[best_strategy][\"annual_return\"]:.4f}\\n')\n        f.write(f'- Sharpe ratio: {extended_metrics[best_strategy][\"sharpe_ratio\"]:.4f}\\n')\n        f.write(f'- Max drawdown: {extended_metrics[best_strategy][\"max_drawdown\"]:.4f}\\n\\n')\n        \n        f.write('### Strategy Recommendations\\n')\n        f.write('- Based on historical data, FunSearch-optimized strategies perform well\\n')\n        f.write('- Consider dynamically adjusting strategy parameters based on market conditions\\n')\n        f.write('- Regularly retrain models to adapt to market changes\\n')\n        f.write('- Implement risk management mechanisms to control maximum drawdown\\n')\n    \n    print(f\"Report saved to: {report_path}\")\n\n"
      }
    ],
    "best_sharpe_ratio": 0.7563842929140105,
    "average_sharpe_ratio": 0.7563842929140104,
    "best_island_id": 0,
    "best_final_nav": 14804386.778134957
  }
]